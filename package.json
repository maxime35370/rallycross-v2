{
  "name": "rx-chrono",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node tools/smoke/smoke.mjs",
    "smoke:projection": "node tools/smoke/projectionSmoke.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-rallycross-rules \"vitest run --config vitest.rules.config.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.28.1",
    "playwright": "^1.62.1",
    "vitest": "^3.1.1"
  }
}
