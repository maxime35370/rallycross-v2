{
  "//": "Configuration de l'ÉMULATEUR uniquement. Aucune section `hosting` : ce fichier ne déploie rien et ne change pas l'hébergement Netlify. Il sert à `npm run test:rules`.",
  "firestore": {
    "rules": "firestore.rules"
  },
  "emulators": {
    "firestore": {
      "port": 8080,
      "host": "127.0.0.1"
    },
    "ui": {
      "enabled": false
    },
    "singleProjectMode": true,
    "auth": {
      "port": 9099,
      "host": "127.0.0.1"
    }
  }
}
